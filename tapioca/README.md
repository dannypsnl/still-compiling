# tapioca (racket implementation)

This version use nanopass, and found an interesting usage that create some middle forms for type checking can pattern match different form at once (e.g. `Type` and `Expr` for check).
